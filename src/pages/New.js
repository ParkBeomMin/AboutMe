export default class New {
    constructor({ $target }) {
        this.$target = $target;
    }
    render() {
        // new CssImport({ cssName: 'New' });
        // new AboutMe({ $target: this.$target }).render();
    }
}
